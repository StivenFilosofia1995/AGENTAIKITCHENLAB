[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn backend:fastapi_app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/status"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
